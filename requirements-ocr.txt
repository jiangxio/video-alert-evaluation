# OCR识别依赖（选择其中一个即可）

# 方案1: PaddleOCR (推荐，中文英文识别效果好)
paddleocr>=2.7
paddlepaddle>=2.5

# 方案2: EasyOCR
easyocr>=1.7

# 方案3: Tesseract
pytesseract>=0.3
# (需要先安装系统包: sudo apt install tesseract-ocr)

# 基础依赖
Pillow>=9.0
