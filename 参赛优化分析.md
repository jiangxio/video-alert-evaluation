# 参赛优化分析报告

> 对照赛事评分标准，逐项评估本项目现状得分、差距与优化方案。
> 评分总分 100 = 通用评测指标 40 + 业务专项指标 60。

---

## 一、评分项逐项对照表

### 通用评测指标（40 分）

| 评分项 | 满分 | 现状 | 现状估分 | 优化目标 | 难度 |
|--------|------|------|----------|----------|------|
| 稳定性-核心功能（扣分制） | 12 | 样本导入/指标计算/结果输出/报告生成四环节均已实现，逻辑完整 | 10~12 | 12 | 中（需测试加固） |
| 稳定性-结果一致性 | 8 | 评测基于 DB 持久化 OCR 结果做确定性计算，不重跑 OCR | 8 | 8 | 低（需验证固化） |
| 易用性-部署与配置 | 6 | ❌ 无 Docker/一键脚本，需手装 ffmpeg+EasyOCR 模型+字体 | 3 | 6 | 中 |
| 易用性-文档完整性 | 5 | 有 README/WEB_PLATFORM_README，缺故障排查与接入说明 | 3 | 5 | 低 |
| 易用性-交互友好性 | 5 | CLI + 50+ REST API + Flask 可视化页面，三种齐全 | 5 | 5 | — |
| 创新性 | 4 | 视频水印+OCR 基准+AI 助手+自动标注，贴合视频流算法评测痛点 | 2~4 | 4 | 中（需包装论述） |
| **小计** | **40** | | **31~35** | **40** | |

### 业务专项指标（60 分）

| 评分项 | 满分 | 现状 | 现状估分 | 优化目标 | 难度 |
|--------|------|------|----------|----------|------|
| 工具落地可用性 | 20 | 流程闭环完整（任务创建→样本导入→评测计算→结果输出→报告生成），且有 yanshou 真实验收报告佐证业务贴合 | 18~20 | 20 | 低 |
| 测试数据丰富性 | 10 | 取决于主办方样本筛选召回率，需现场操作 | 6~10 | 10 | 中（依赖数据） |
| 标注结果一致性（MAPE） | 12 | 指标计算逻辑严谨（命中判定/精确率/召回率有完整文档） | 10~12 | 12 | 中（需对齐口径） |
| 评测资源消耗（镜像大小） | 3 | EasyOCR+ffmpeg+依赖，预估 2~5 GB | 2 | 3 | 中（需瘦身） |
| LLM/VLM token 消耗 | 3 | ⚠️ VLM 自动标注 + LLM 报告摘要，单次全量 token 易破万 | 0~1 | 3 | 高（需重构策略） |
| 模型类型（三选一，选视觉类） | 12 | 视频事件检测、视觉异常检测稳定支持（✅ 已定务实路线，不扩展） | 4 | 4 | — |
| **小计** | **60** | | **40~51** | **60** | |

**现状合计估分：约 71~86 / 100；优化目标：90+ / 100。**

---

## 二、高 ROI 优化清单（按优先级排序）

| 优先级 | 优化项 | 预估提分 | 难度 | 说明 |
|--------|--------|----------|------|------|
| P0 | Docker 化部署 | +3 | 中 | 部署从 3→6 分，最高性价比 |
| P0 | 补全四类文档 | +2 | 低 | 安装/接入/使用/故障排查 |
| P1 | LLM/VLM token 策略重构 | +2~3 | 高 | 厘清核心评测不依赖 LLM，控制辅助 token |
| P1 | 镜像瘦身 | +1 | 中 | 多阶段构建 + 模型按需下载，力争 <2GB |
| P1 | 结果一致性验证固化 | 保 8 | 低 | 写重复评测脚本，输出偏差报告 |
| P1 | 核心功能测试加固 | 保 12 | 中 | 异常输入测试，防扣分 |
| P2 | 创新点论述包装 | +1~2 | 中 | 文档化命题价值与创新优化点 |
| P2 | 标注一致性口径对齐 | 保 12 | 中 | 与人工真值计算口径逐项核对 |

---

## 三、重点优化方案详解

### P0-1：Docker 化部署（部署 3→6 分）

**现状问题**：当前部署需手动安装 ffmpeg、下载 EasyOCR 模型（数百 MB）、配置字体路径，跨平台易踩坑。评分标准明确"Docker 镜像或一键启动脚本 + 仅改 1 个配置文件 = 6 分"。

**方案**：
1. 新增 `Dockerfile`（多阶段构建）：
   - 基础镜像 `python:3.11-slim`
   - 系统层装 `ffmpeg` + `fonts-dejavu`（解决字体依赖）
   - Python 依赖层（利用缓存）
   - EasyOCR 模型预下载层（避免首次运行下载）
2. 新增 `docker-compose.yml`：一键起服务 + 数据卷挂载（uploads/benchmark.db/ground_truth）
3. 新增 `.env` 作为**唯一配置文件**（API key、端口、模型路径），对应评分"仅改 1 个配置文件"
4. 提供 `make up` / `docker compose up -d` 一键启动

**收益**：部署项 3→6 分；同时镜像大小项可一并优化。

---

### P0-2：补全四类文档（文档 3→5 分）

**现状问题**：README 偏功能介绍，缺系统级"接入"与"故障排查"。评分要求"安装、接入、使用、故障排查"四类齐全 = 5 分。

**方案**：在 `docs/` 下补全：
- `docs/install.md` — 安装（Docker 一键 + 手动两种方式，含 ffmpeg/字体/模型）
- `docs/integration.md` — 接入（如何对接公司 AI 产品：算法版本注册、告警数据格式、ground_truth 导入规范）
- `docs/usage.md` — 使用（CLI + Web 全流程操作手册，含截图）
- `docs/troubleshooting.md` — 故障排查（OCR 识别失败、字体缺失、端口冲突、模型下载慢等常见问题与解法）

**收益**：文档项 3→5 分。

---

### P0-3：视觉类任务类型 —— 已决策：务实保 4 分（不再扩展）

**评分规则**：视觉类任务每正确支持一类得 2 分，6 类封顶 12 分（图像分类、目标检测、语义分割、关键点检测、视频事件检测、视觉异常检测）。

**决策结论**：只稳保视频事件检测 + 视觉异常检测 = **4 分**，不扩展图像分类/目标检测/语义分割/关键点检测。

**决策依据**：

1. **逐帧标注不现实**：项目基于视频流，图像分类/目标检测若用现有视频评测需对每帧标注类别/画框，工作量几乎不可能。
2. **标注成本可转嫁，但开发成本仍在**：图像分类评测可用开源带标数据集（如花卉/Caltech 子集）演示，不必逐帧标；但真实瓶颈是需新开一个脱离视频水印流程的通用评测入口（导入预测/真值 CSV，算准确率/F1），开发半天~1 天。目标检测更重（mAP/IoU，2~3 天且易错）。
3. **性价比与风险权衡**（核心理由）：
   - 这 12 分是"选"项，每类才 2 分，边际 ROI 低。
   - 强行堆多类视觉任务会稀释"视频流算法评测"命题聚焦度，反噬**落地可用性 20 分**（评分要求"贴合公司 AI 产品常态化评测场景"）和**创新性 4 分**。为多拿 2~4 分冒险影响 24 分，不划算。
   - 同样精力投到 Docker(+3)、文档(+2)、token(+3) 更值，且不分散主题。

**任务类型取舍明细**：

| 评分标准视觉类任务 | 项目对应能力 | 决策 |
|---|---|---|
| 视频事件检测 | ✅ 已支持（告警与 GT 事件时间窗口匹配） | 保留，稳拿 2 分 |
| 视觉异常检测 | ✅ 已支持（误检/漏检判定） | 保留，稳拿 2 分 |
| 图像分类 | 需新开通用 CSV 评测入口 + 开源数据集 | ❌ 不做 |
| 目标检测 | 需实装 mAP/IoU，易错 | ❌ 不做 |
| 语义分割 | 开发与标注成本高 | ❌ 不做 |
| 关键点检测 | 开发与标注成本高 | ❌ 不做 |

**收益**：视觉类稳拿 4 分；通过保持命题聚焦度，间接保护落地可用性 20 分 + 创新性 4 分。

---

### P1-1：LLM/VLM token 策略重构（token 0→3 分）

**现状问题**：评分规定"单次评测（一个算法）消耗 token 数 ≤1000 = 3 分；>10000 = 0 分；未使用 LLM/VLM = 0 分"。当前：
- VLM（Qwen3-VL/Auto-Multi）用于自动标注，对**每张告警图片**调用，全量数据集 token 极易破万
- LLM（Claude）用于报告摘要/结论生成（`_call_claude`）

**关键认知**：评分是"辅助评估"才计入。本项目**核心评测指标（精确率/召回率/误检率）的计算完全不依赖 LLM/VLM**——OCR 用本地 EasyOCR，指标计算是纯 Python 确定性逻辑。LLM/VLM 仅用于：
1. 自动标注（可选辅助，非评测必需）
2. 报告摘要生成（可选辅助）

**方案**（三选一或组合）：
1. **论证策略**：明确"核心评测零 LLM token"，自动标注/报告摘要为可选增强项，单独计量。在文档与报告中讲清架构边界。
2. **token 预算控制**：报告摘要复用单次 Claude 调用（输入评测指标 JSON，输出 200~300 字摘要），单次约 500~1000 token，可压到 ≤1000 = 3 分。自动标注默认关闭，按需启用。
3. **缓存复用**：报告摘要结果缓存，避免重复评测重复消耗。

**收益**：token 项 0→3 分；同时让架构更清晰。

---

### P1-2：镜像瘦身（资源消耗 2→3 分）

**方案**：
- 多阶段构建：构建期装编译依赖，运行期只拷必要产物
- 基础镜像用 `python:3.11-slim` 而非完整版
- EasyOCR 模型单独挂载卷或按需下载，不打入镜像
- 清理 pip 缓存、`__pycache__`
- 目标：镜像 <2GB = 3 分

---

### P1-3：结果一致性验证固化（保 8 分）

**现状优势**：评测执行从 `ocr_results` 表读取**已持久化**的 OCR 结果做命中判定，不重跑 OCR。指标计算（命中/精确率/召回率）是确定性逻辑，同输入重复评测偏差应为 0%。

**方案**：
1. 写 `scripts/consistency_check.py`：对同一任务重复执行 finalize 5 次，输出指标偏差报告
2. 在报告中固化该结论：偏差 ≤2% = 8 分
3. 注意：OCR 阶段若有随机性需单独说明——OCR 是预跑缓存的，评测阶段确定性

**收益**：稳拿一致性 8 分。

---

### P1-4：核心功能测试加固（保 12 分）

**现状风险**：核心功能 12 分是扣分制（样本导入异常扣 3、指标计算异常扣 3、结果输出异常扣 3、报告生成异常扣 3）。

**方案**：针对四环节写异常输入测试（`tests/`）：
- 样本导入：畸形 ZIP、空数据集、非法文件名
- 指标计算：空 GT、confirmed_count=0、无命中场景
- 结果输出：空结果、超大数据集
- 报告生成：未完成评测即生成、缺失字段

**收益**：保住核心功能 12 分，防现场翻车。

---

### P1-5：标注结果一致性核对（保 12 分）✅ 已完成

**评分项**：系统计算指标 vs 人工标注真值标准结果，加权 MAPE。MAPE≤2% → 12 分。

**核对方式**：用 `scripts/label_consistency_check.py`，将平台 `compute_task_metrics` 输出与《视频流算法验证报告》（`yanshou/`，人工逐条核对基准）对比。

**核对结论**（任务6「0528」，对应验收数据集）：

| 指标 | 口径 | MAPE | 判定 |
|------|------|------|------|
| 精确率 | (告警-误检)/告警 | **0.89%** | ✅ 口径正确，达标 |
| 召回率 | 各类型算术平均（宏平均） | **3.69%**（多数类型<2%） | ✅ 口径正确，达标 |
| 误检/h | 各类型算术平均（宏平均） | 9.32%（对齐8类后） | ⚠️ 口径正确，差异源于数据快照 |

**关键发现**：
1. **三项口径均正确**，与验收报告/realtime 模式一致。普通模式误检/h 实际走 `compute_overall_avg_fp()`（算术平均），非合计/时长。
2. 误检/h 单类型 MAPE 偏高（rat 69%、mask 105%）是**数据快照差异**：任务6当时人工确认未到验收终稿（rat 误检平台4 vs 验收13）。属数据问题，非计算逻辑问题。
3. 修正了 CLAUDE.md 对误检/h 口径的过时记录（原记"合计/时长"，实际为算术平均）。

**收益**：口径已验证正确，标注一致性 12 分有据可保。核对脚本可复现。

---

### P2：创新点论述包装（创新 2→4 分）

评分创新性两档可叠加（贴合业务痛点 4 分 + 创新性优化 2 分，但封顶 4 分）。

**论述要点**：
1. **命题贴合业务痛点**：视频流 AI 算法（跌倒、打电话、明火、口罩等）常态化评测是公司 AI 产品迭代的真实痛点，本工具直接服务于算法版本验证（yanshou 目录有真实落地报告佐证）。
2. **创新性优化**：
   - 水印基准法：用 OCR 提取视频 ID+时间戳实现告警与 GT 的自动对齐，免去人工逐帧标注
   - 低资源：核心评测不依赖 GPU/大模型，本地 EasyOCR 即可
   - 快速适配：告警类型 JSON 配置化，新增算法类型零代码
3. 在参赛材料/报告中显式呈现以上创新点。

---

## 四、风险点与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 现场测试数据筛选召回率不足 | 测试数据丰富性丢分 | 提前研究主办方样本格式，确保筛选覆盖全 |
| 标注一致性 MAPE 偏差 | 12 分大项 | 与人工真值计算口径逐项核对（confirmed_count/actual_count/min 封顶逻辑） |
| OCR 现场识别率下降 | 核心功能+一致性 | 预处理参数已固化（裁剪/灰度/对比度/反色），确保水印清晰 |
| Docker 镜像过大 | 资源消耗+部署双丢分 | 多阶段构建+模型外挂 |
| LLM token 超标 | token 项 0 分 | 核心评测零 LLM，辅助项按预算控制 |

---

## 五、推荐实施顺序

```
第一阶段（保底分，1~2 天）：
  1. 补全四类文档（+2）
  2. 结果一致性验证脚本（保 8）
  3. 核心功能异常测试（保 12）

第二阶段（拉分项，2~3 天）：
  4. Docker 化部署 + 镜像瘦身（+3+1）
  5. LLM token 策略重构（+2~3）

第三阶段（包装，1 天）：
  6. 创新点论述 + 参赛报告包装
  7. 标注一致性口径核对

> 视觉类任务类型已决策：务实保 4 分（视频事件检测+异常检测），不扩展，详见 P0-3 节。
```

**预期效果**：现状约 71~86 分 → 优化后 90+ 分。

---

## 六、需要你决策的点

1. ~~**视觉类任务类型**~~ ✅ 已决策：务实保 4 分（视频事件检测 + 视觉异常检测），不扩展图像分类/目标检测等。
2. **LLM token 策略**：选"论证核心评测零 LLM"还是"压预算到 ≤1000 token"？自动标注功能参赛时是否默认关闭？
3. **Docker 化**：是否现在就开始做？这是最高 ROI 项，但需确认目标镜像大小预期。
4. **实施范围**：以上优化全部做，还是只挑高 ROI 的 P0 项？
