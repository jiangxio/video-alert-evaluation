# 平台功能优化建议

> 基于代码现状分析，2026-07-22
> 分析对象：视频水印 OCR 评测平台（视频上传 → 水印 → 告警数据集 → OCR → 评测 → 自动标注 → 推流）

---

## 一、平台现状速览

| 模块 | 状态 | 备注 |
|---|---|---|
| 视频上传 / 管理 / 水印 | ✅ 完善 | `videos.py`、`watermark_service.py` |
| 告警数据集 + 批量 OCR | ✅ 完善 | `alerts.py` 已用 `threading` 后台化，有进度接口 |
| 评测（二次确认 → 执行 → finalize → 导出 PDF） | ✅ 核心完整 | **缺可视化**，`eval_task.html` 90KB 但无任何图表 |
| 自动标注 | ✅ 已有 | `auto_annotation.py` |
| 推流 | ✅ 已有 | `streaming.py` |
| 算法管理 / 算法类型 / 事件类型 | ✅ 已有 | `algorithms.py`、`event_types.py` |
| AI 助手（function calling + 二次确认 + 审计日志） | ✅ 完整 | `assistant_service.py`、`assistant_tasks.py` |

**核心缺口**集中在**评测结果的表达层**和**多任务的横向洞察**——这对一个 benchmark 评测平台是价值最高的地方。

### 导航结构（base.html）

```
首页 → 视频上传 → 视频管理 → 告警数据集 → 评测 → 自动标注 → 推流
    → 算法管理 → 算法类型 → AI 助手设置
```

### 已有但未充分利用的数据基础

- `algorithm_versions` 表：可支撑版本对比，但前端无对比视图
- `/api/tasks/<id>/event-metrics` 接口：已有各事件类型指标，前端未可视化
- `_get_effective_status` + `manual_status` 机制：支持人工改判，但无批量复核工作台
- `assistant_audit_log`：已有审计能力，可低成本扩展到全平台操作审计

---

## 二、建议清单（按性价比排序）

### 🥇 P0 — 评测结果可视化（最明显的缺口）

**现状**：`eval_task.html` 有 90KB、几十个 API，但 `grep Chart|echarts|canvas` **零命中**。精确率 / 召回率 / 误检每小时全是纯文本数字。

**建议**：

1. **各事件类型召回率柱状图**
   - 后端已有 `/api/tasks/<id>/event-metrics`，前端直接用 echarts 渲染
   - 一眼看出哪类事件拉低整体召回

2. **命中 / 误检时间轴**
   - 把告警窗口和 GT 事件区间画在同一条时间轴上
   - 红 = 误检、绿 = 命中、灰 = 漏检
   - 比表格直观 10 倍，且能辅助确认 `confirmed_count` 调整

3. **混淆分布**
   - 误检告警按事件类型 / 视频分布的饼图

**价值**：直接提升评测结果的可读性，是当前最易感知的体验提升。

---

### 🥇 P0 — 长任务进度自动反馈

**现状**：`evaluation.html` 列表页加载后**无 `setInterval` 轮询**，任务状态变更需手动刷新。后端用 `threading` 跑长任务，用户不知道何时完成。

**建议**：

1. 列表页对 `created / evaluating / finalizing` 状态行轮询 `/api/tasks`（5–10s 间隔），`done` 后停止轮询
2. 复用 CLAUDE.md 里的 `notify-send`：任务完成时桌面提醒 + 浏览器 `Notification`

**价值**：消除"不知道任务跑没跑完"的焦虑，减少无意义手动刷新。

---

### 🥈 P1 — 多任务 / 多版本横向对比

**现状**：这是评测平台的核心价值，但 `evaluation.html` 里"对比"只出现 1 次，无实际功能。

**建议**：

1. **同算法不同版本趋势线**
   - `algorithm_versions` 表已存在
   - 按版本画精确率 / 召回率趋势，回答"新版有没有退步"

2. **多任务指标对比表**
   - 勾选 ≥2 个任务，并排展示 P / R / 误检每小时，高亮最优

3. **A/B 对比详情**
   - 选两个任务，逐事件类型 diff 召回率，定位回归点

**价值**：让平台从"单次评测"升级为"持续 benchmark"，支撑算法迭代决策。

---

### 🥈 P1 — 误检样本快速复核工作台

**现状**：`finalize` 后误检告警散落在结果表里，逐张点开复核很慢。

**建议**：

- **误检专项视图**：左侧缩略图列表（按事件类型分组），右侧大图 + OCR 结果 + 时间轴上下文
- **键盘快捷键**：`←→` 翻页、`f` 标误检、`c` 改判正确、`i` 忽略，批量过
- 已有 `_get_effective_status` 和 `manual_status` 机制，前端串起来即可

**价值**：将"逐张点开复核"的线性操作压缩为"键盘流水线复核"，效率提升一个量级。

---

### 🥉 P2 — 评测配置模板化

**现状**：每次新建评测要重选视频集 / 告警集 / 容差参数。

**建议**：

- 把常用组合存为模板，一键加载
- 数据量小，新增一张 `eval_task_templates` 表即可

**价值**：减少重复配置，降低误操作。

---

### 🥉 P2 — 任务可恢复性

**现状**：后台用 `threading`（`watermark_service.py`、`alerts.py`），**服务重启则任务丢失**。

**建议**：

- 若评测 / OCR 耗时长，把任务状态持久化到 DB
- 重启后能恢复，或至少标记为"中断"
- 按实际使用频率决定是否上 Celery

**价值**：提升健壮性，避免长任务白跑。

---

### 🥉 P3 — 多用户与操作审计

**现状**：当前无登录系统，多人同时 finalize 同一任务会互相覆盖 `manual_status`。

**建议**：

- 加一层轻量用户标识 + 关键操作审计
- 已有 `assistant_audit_log` 可复用扩展

**价值**：支撑多人协作场景，操作可追溯。

---

## 三、推荐实施顺序

| 阶段 | 任务 | 预期工作量 | 收益 |
|---|---|---|---|
| 第一阶段 | P0 评测可视化 + P0 进度自动反馈 | 中（前端为主，复用现有 API） | 高，立竿见影 |
| 第二阶段 | P1 多版本对比 + P1 误检复核工作台 | 中高 | 高，核心价值 |
| 第三阶段 | P2 模板化 + P2 任务恢复 | 中 | 中，体验打磨 |
| 第四阶段 | P3 多用户审计 | 中低 | 看是否多人协作 |

**建议从 P0 的评测可视化入手**：后端 API 齐备，主要是前端 echarts 集成，改动集中在 `eval_task.html`，风险低、见效快。

---

## 四、附：关键代码位置参考

| 关注点 | 文件 / 符号 |
|---|---|
| 评测指标核心逻辑 | `app/routes/evaluation.py`（`finalize_task`、`_get_effective_status`） |
| 评测结果页 | `app/templates/eval_task.html`（90KB，无图表） |
| 评测列表页 | `app/templates/evaluation.html`（无状态轮询） |
| 各事件类型指标接口 | `/evaluation/api/tasks/<id>/event-metrics` |
| 详细报告导出 | `/evaluation/api/tasks/<id>/detailed-report`（已支持 PDF） |
| 后台任务机制 | `threading`（`watermark_service.py`、`alerts.py`、`assistant_tasks.py`） |
| 算法版本数据 | `algorithm_versions` 表 |
| 人工改判机制 | `manual_status`（correct/false_positive/ignored/auto） |
| 审计日志 | `assistant_audit_log` 表 |

---

*本文档由代码现状分析生成，可作为后续迭代规划的依据。*
